---
# Linux victims: use the same key as Terraform (CharlieSpring26) or set user/key.
# Ubuntu cloud images: ansible_user=ubuntu. Debian: often debian or admin — set in inventory if needed.
# Set the SSH key that matches the OpenStack key_pair used by Terraform (e.g. CharlieSpring26).
# Example (on Kali): /home/kali/.ssh/CharlieSpring26
# ansible_ssh_private_key_file: /path/to/CharlieSpring26_private_key

---
# Author: Caroline Richards
# Email: cpr9499@rit.edu

# This playbook deploys a web stack on a Linux host with Nginx, MariaDB, and PHP

- name: Install Nginx, MariaDB, and PHP packages
  apt: 
    name: 
      - nginx
      - mariadb-server
      - php-fpm
      - php-mysql
    state: present
    update_cache: yes

# Nginx Setup
- name: Start and enable Nginx
  service: 
    name: nginx
    state: started
    enabled: yes

- name: Ensure web root directory exists
  file:
      path: "{{ web_stack.web_root }}"
      state: directory
      owner: www-data
      group: www-data
      mode: '0755'

- name: Copy website files
  copy: 
    src: "{{ item }}"
    dest: "{{ web_stack.web_root }}/{{ item }}"
    owner: www-data
    group: www-data
    mode: '0644'
  loop:
    - index.php
    - config.php
    - style.css
    - score.php

- name: Copy Nginx configuration
  copy: 
    src: nginx.conf
    dest: /etc/nginx/sites-available/miracle_ice
    mode: '0644'

- name: Enable nginx site
  file: 
    src: /etc/nginx/sites-available/miracle_ice
    dest: /etc/nginx/sites-enabled/miracle_ice
    state: link

- name: Remove default nginx site
  file: 
    path: /etc/nginx/sites-enabled/default
    state: absent

- name: Restart Nginx 
  service: 
    name: nginx
    state: restarted

# MariaDB Setup
- name: Start and enable MariaDB
  service:
    name: mariadb
    state: started
    enabled: yes

- name: Copy SQL setup script
  copy:
    src: mariadb_setup.sql
    dest: /tmp/mariadb_setup.sql
    mode: '0644'

- name: Run SQL setup script
  mysql_db:
    name: miracle_ice
    state: import
    target: /tmp/mariadb_setup.sql

- name: Create Database user
  mysql_user:
    name: "{{ web_stack.mariadb.app_user }}"
    password: "{{ web_stack.mariadb.app_password }}"
    priv: "miracle_ice.*:ALL"
    state: present

# PHP
- name: Start and enable PHP-FPM
  service:
    name: php8.1-fpm
    state: started
    enabled: yes